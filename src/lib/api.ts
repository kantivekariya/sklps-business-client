/**
 * Centralized API client. Use this instead of direct axios/fetch.
 * @see api-client.ts for implementation
 * @see api-endpoints.ts for endpoint constants
 */
export { api as default, API_ENDPOINTS } from "./api-client";
